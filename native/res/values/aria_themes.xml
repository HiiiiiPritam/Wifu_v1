<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Self-contained (doesn't depend on the RN-generated app theme) --
         used only by IncomingCallActivity and InCallActivity. -->
    <style name="Theme.AriaCompanion.Call" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowBackground">@color/call_bg</item>
        <item name="android:statusBarColor" tools:targetApi="21">@color/call_bg</item>
    </style>
</resources>
