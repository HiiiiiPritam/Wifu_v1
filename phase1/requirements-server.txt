# Call server only (a cloud VM). The desktop companion's extras --
# sounddevice, pyvts, soundfile, webrtcvad, numpy -- are for a PC with a
# mic, speakers and VTube Studio, and faster-whisper is a ~200MB download
# for a model the server doesn't run: speech-to-text happens on Groq
# (settings.stt_engine). Installing those on a 1GB VM wastes disk and RAM.
#
#   pip install -r requirements-server.txt
#
# Note: with these packages, settings.stt_engine = "local" won't work --
# transcription is Groq-only here.
groq>=1.7.0
python-dotenv==1.0.1
edge-tts==7.2.8
fastapi==0.115.6
uvicorn[standard]==0.34.0
cryptography==44.0.0
firebase-admin==6.5.0
psutil==7.2.2
# Mood-matched fillers (call/mood.py): tiny, pure Python, no model download.
vaderSentiment==3.3.2
